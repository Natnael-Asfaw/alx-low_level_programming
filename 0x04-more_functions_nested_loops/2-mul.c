#include "main.h"
/**
*mul - multiples two integers
*@i: int to be multiplied to n
*@n: int to be multi to i
*Return: resultof the operation
*/
int mul(int i, int n)
{
int mul;
mul = i + n;
return (mul);
}
