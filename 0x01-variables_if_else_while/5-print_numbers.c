#include <stdio.h>
/**
*main - prints all single digit num of 10
*starting from 0 followed by new line
*Return: 0
*/
int main(void)
{
int i;
for (i = 0; i < 10; i++)
printf("%d", i);
printf("\n");
return (0);
}
