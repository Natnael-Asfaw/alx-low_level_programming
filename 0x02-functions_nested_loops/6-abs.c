#include "main.h"
#include <stdio.h>
/**
*_abs - computes the absolute value of an
*integer
*@i: int
*Return: the absolute value of int
*/
int _abs(int i)
{
if (i >= 0)
{
return (i);
}
return (-i);
}
