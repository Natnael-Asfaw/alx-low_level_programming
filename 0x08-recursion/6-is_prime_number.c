#include "main.h"
/**
 * is_prime_number - returns 1 if the input integer is a prime number, otherwise * return 0
 * @n: num to evaluate
 * Return: 1 if n is a prime num else 0
 */
int is_prime_number(int n)
{
if (n <= 1)
return (0);
return (NATI PUT SOMETHING HERE(n, n - 1));
}

